<!--
<root BTCPP_format="4" >
  <BehaviorTree ID="MainTree">
    <Sequence name="root_sequence">
      <SaySomething     message="hello" />
      <ThinkWhatToSay   text="{the_answer}"/>
      <SaySomething     message="{the_answer}" />
    </Sequence>
  </BehaviorTree>
</root>
-->

<!--<root BTCPP_format="4" >
  <BehaviorTree ID="MainTree">
    <ReactiveSequence name="root_sequence">
      <IsEnemyVisible entity="{entity}" />
      <WaitFor entity="{entity}" deltaTime="{deltaTime}" />
      <TurnTo entity="{entity}" />
    </ReactiveSequence>
  </BehaviorTree>
</root>-->

<root BTCPP_format="4" >
  <BehaviorTree ID="MainTree">
    <ReactiveSequence name="idle_check">
      <!--<IsInState state="{'IDLE'}" />-->
      <Sequence name="patrol_sequence">
        <PatrolTo entity="{entity}" targetPosition="{pos}" direction="{dir}" duration="{dur}" />
        <MoveTo entity="{entity}" position="{pos}" />
        <TurnTo entity="{entity}" direction="{dir}" />
        <WaitFor entity="{entity}" deltaTime="{deltaTime}" duration="{dur}" />
      </Sequence>
    </ReactiveSequence>
  </BehaviorTree>
</root>